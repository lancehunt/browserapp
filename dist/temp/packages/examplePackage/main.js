define([
		'require',
        'core',
        'i18n!./nls/resource',
    ], function ( require, core, resource ) {
		core.loadCss(require.toUrl('./less/package.css'));	
		
		return {
			// TODO: do something interesting
		};
	});